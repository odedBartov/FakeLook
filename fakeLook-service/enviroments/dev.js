// const dotenv = require('dotenv');

// const res = dotenv.config();

// if(res.error){
//     console.log('could not get enviroment vriables. ');
//     throw error;
// }
// else console.log(res.parsed);

// module.exports = res.parsed;

module.exports = {
    secretKey: 'fakeLookSecret'
}